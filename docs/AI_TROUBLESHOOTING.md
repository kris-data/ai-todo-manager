# AI 할 일 생성 문제 해결 가이드

## ❌ 일반적인 오류 및 해결 방법

### 1. "models/gemini-1.5-flash is not found" 오류

**증상:**
```
Error: models/gemini-1.5-flash is not found for API version v1beta
```

**원인:**
- 모델 이름이 올바르지 않음
- Gemini API의 모델 이름 형식이 변경됨

**해결 방법:**
✅ **이미 수정 완료!** 모델 이름을 `gemini-1.5-flash-latest`로 변경했습니다.

**다른 모델 옵션:**
```typescript
// 빠른 응답 (권장)
model: google('gemini-1.5-flash-latest')

// 더 정확한 결과
model: google('gemini-1.5-pro-latest')

// 안정 버전
model: google('gemini-pro')
```

---

### 2. "AI 서비스 설정이 올바르지 않습니다" 오류

**원인:**
- `GOOGLE_GENERATIVE_AI_API_KEY` 환경 변수가 설정되지 않음

**해결 방법:**
1. `.env.local` 파일 확인
2. 다음 추가:
   ```env
   GOOGLE_GENERATIVE_AI_API_KEY=your_api_key_here
   ```
3. 서버 재시작:
   ```bash
   # Ctrl+C로 중지 후
   npm run dev
   ```

---

### 3. "AI 서비스 인증에 실패했습니다" 오류

**원인:**
- API 키가 올바르지 않음
- API 키가 만료됨
- API 키 권한 부족

**해결 방법:**
1. Google AI Studio에서 키 확인:
   - https://makersuite.google.com/app/apikey
2. 새 API 키 생성
3. `.env.local` 업데이트
4. 서버 재시작

---

### 4. "사용량 초과" 오류

**원인:**
- Gemini API 무료 한도 초과
- Rate limit 도달

**해결 방법:**
1. **잠시 대기** (1분 후 재시도)
2. **무료 한도 확인**:
   - https://ai.google.dev/pricing
3. **유료 플랜 고려**:
   - Google Cloud Console에서 결제 설정

**무료 한도 (2024년 기준):**
- 분당 15 요청
- 일일 1,500 요청
- 월 100만 토큰

---

### 5. API 응답이 느림

**증상:**
- AI 변환에 5초 이상 소요

**원인:**
- 네트워크 지연
- 복잡한 입력
- 서버 부하

**해결 방법:**
1. **입력 단순화**:
   ```
   ❌ "내일 오후 3시에 중요한 팀 프로젝트 회의 준비를 위해 자료 정리하고 발표 자료 만들기"
   ✅ "내일 오후 3시 팀 회의 준비"
   ```
2. **더 빠른 모델 사용**:
   ```typescript
   model: google('gemini-1.5-flash-latest') // 가장 빠름
   ```
3. **네트워크 확인**

---

### 6. 변환 결과가 이상함

**예시:**
- 날짜가 잘못 파싱됨
- 우선순위가 예상과 다름
- 카테고리가 적절하지 않음

**해결 방법:**
1. **더 명확하게 입력**:
   ```
   ❌ "회의"
   ✅ "내일 오후 2시 클라이언트 미팅"
   ```

2. **키워드 사용**:
   - 우선순위: "긴급", "중요", "꼭"
   - 날짜: "내일", "다음주 월요일", "2026년 2월 15일"
   - 시간: "오전 9시", "저녁 7시"

3. **AI 변환 후 수동 수정**:
   - AI는 제안일 뿐, 폼에서 직접 수정 가능

---

### 7. TypeScript 타입 오류

**증상:**
```
Type 'string | undefined' is not assignable to type 'string'
```

**해결 방법:**
✅ **이미 처리됨** - API Route에서 모든 타입 체크 완료

---

### 8. 환경 변수가 읽히지 않음

**증상:**
- API 키를 설정했는데도 "설정이 올바르지 않습니다" 오류

**원인:**
- 서버가 재시작되지 않음
- 파일명이 잘못됨 (`.env.local.example` vs `.env.local`)
- 환경 변수 이름 오타

**해결 방법:**
1. **파일명 확인**: `.env.local` (예제 파일 아님!)
2. **변수명 확인**: `GOOGLE_GENERATIVE_AI_API_KEY` (정확히)
3. **서버 재시작**:
   ```bash
   # 터미널에서 Ctrl+C
   npm run dev
   ```
4. **브라우저 새로고침**: `Ctrl+Shift+R` (캐시 무시)

---

## 🔍 디버깅 체크리스트

### 서버 사이드
- [ ] `.env.local` 파일 존재
- [ ] `GOOGLE_GENERATIVE_AI_API_KEY` 설정됨
- [ ] API 키 유효함 (Google AI Studio 확인)
- [ ] 서버 재시작함
- [ ] 터미널에 에러 메시지 없음

### 클라이언트 사이드
- [ ] 브라우저 콘솔에 에러 없음
- [ ] 네트워크 탭에서 API 호출 확인
- [ ] 토스트 알림 메시지 확인

### API 호출
- [ ] `/api/ai/parse-todo` 응답 200
- [ ] 요청 body에 `input` 포함
- [ ] 응답 body에 `data` 포함

---

## 🧪 테스트 방법

### 1. API 직접 테스트

브라우저 콘솔에서:
```javascript
fetch('/api/ai/parse-todo', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ input: '내일 오후 3시 회의' })
})
.then(r => r.json())
.then(console.log)
```

**예상 결과:**
```json
{
  "success": true,
  "data": {
    "title": "회의",
    "due_date": "2026-02-10",
    "due_time": "15:00",
    "priority": "medium",
    "category": ["업무"]
  }
}
```

### 2. 터미널 로그 확인

정상 작동 시:
```
POST /api/ai/parse-todo 200 in 3s
AI 파싱 결과: { title: '회의', ... }
```

에러 발생 시:
```
POST /api/ai/parse-todo 500 in 1s
AI 할 일 파싱 오류: Error [AI_APICallError]: ...
```

---

## 💡 팁

### 1. 개발 중 빠른 재시작
```bash
# nodemon 사용 (자동 재시작)
npm install -D nodemon
npx nodemon --exec "npm run dev"
```

### 2. API 키 확인
```bash
# Linux/Mac
echo $GOOGLE_GENERATIVE_AI_API_KEY

# Windows PowerShell
$env:GOOGLE_GENERATIVE_AI_API_KEY

# .env.local 파일 직접 확인
cat .env.local
```

### 3. 로그 레벨 조정
API Route에서 더 많은 로그 출력:
```typescript
console.log('입력:', input);
console.log('AI 응답:', result);
console.log('변환 완료:', result.object);
```

---

## 📞 추가 도움

### 여전히 해결되지 않나요?

1. **GitHub Issues 확인**:
   - Vercel AI SDK: https://github.com/vercel/ai/issues
   - Google AI SDK: https://github.com/googleapis/google-cloud-node

2. **공식 문서**:
   - Google AI: https://ai.google.dev/docs
   - Vercel AI SDK: https://sdk.vercel.ai/docs

3. **Discord 커뮤니티**:
   - Vercel: https://vercel.com/discord

---

## ✅ 해결 완료!

현재 수정 사항:
- ✅ 모델 이름 수정: `gemini-1.5-flash` → `gemini-1.5-flash-latest`
- ✅ 에러 처리 개선
- ✅ 로그 추가

**이제 다시 시도해보세요!**
1. 페이지 새로고침 (`F5`)
2. AI 변환 버튼 클릭
3. 성공 메시지 확인

문제가 계속되면 터미널의 에러 로그를 확인하고 위 가이드를 참고하세요.
